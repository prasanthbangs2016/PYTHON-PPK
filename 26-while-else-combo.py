import os
os.system("cls")
loopcounter=int(input("Enter number of value:"))

while loopcounter <=3:
  print ("condition is matched entered value is:",loopcounter)
  loopcounter = loopcounter+1
else:
  print ("working in the \"else\"part of loop")